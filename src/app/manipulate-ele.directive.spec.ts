import { ManipulateEleDirective } from './manipulate-ele.directive';

describe('ManipulateEleDirective', () => {
  it('should create an instance', () => {
    const directive = new ManipulateEleDirective();
    expect(directive).toBeTruthy();
  });
});
